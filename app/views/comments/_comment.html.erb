<%= div_for comment do %>
  <p><strong>
    Posted <%= time_ago_in_words(comment.created_at) %> by
    <%= comment.user.first_name %> <%= comment.user.last_name %>
  </strong>
    <br />
      <%= h(comment.body) %>
  </p>
  <p>

<% end %>
